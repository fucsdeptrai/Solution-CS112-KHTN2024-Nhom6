#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1e5;
const int INF = 1e5;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    int n = inf.readInt(1, MAXN, "n");
    inf.readEoln();

    vector<int> a = inf.readInts(n, -INF, INF, "a_i");
    inf.readEoln();
    inf.readEof();
}
